  <nav class="navbar navbar-expand-lg navbar-light bg-light">
    <p class="navbar-text"><%= @merchant.name %></p>
    <ul class="nav navbar-nav">
      <li><%= link_to 'Dashboard', merchant_dashboard_index_path, style: 'pull-right' %>
      <%= link_to 'My Items', merchant_items_path(@merchant), style: 'pull-right' %>
      <%= link_to 'My Invoices', merchant_invoices_path(@merchant), style: 'pull-right' %></li>
      <li><%= link_to 'Discounts', merchant_bulk_discounts_path, class: 'link_1', style: 'pull-right' %>
    </ul>
  </nav>
</div>

<h1>Edit Bulk Discount</h1>
<%= form_with url: "/merchant/#{@merchant.id}/bulk_discounts/#{@discount.id}", method: :patch, local: true do |f| %>
  <%= f.label :percentage_discount %>
  <%= f.text_field :percentage_discount, required: true  %><br/><br/>
  <%= f.label :quantity_threshold %>
  <%= f.text_field :quantity_threshold, required: true  %><br/><br/>
  <%= f.submit 'Save'%>
<% end %>